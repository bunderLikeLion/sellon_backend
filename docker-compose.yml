version: '3'
services:
  webapp:
    env_file:
      - ./.env
    volumes:
      - ./webapp:/usr/src/app
    build:
      context: ./webapp
      dockerfile: ./Dockerfile
    image: webapp-dev-image
    container_name: webapp-dev-container
    entrypoint:
      - /docker-entrypoint.sh
    ports:
      - 8888:8888
